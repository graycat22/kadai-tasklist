<h2>ユーザ一覧</h2>
  <% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
       <li>
          <div>
            <%= user.name %>
          </div>
          <div>
            <p><%= link_to "詳細を見る", user_path(user) %></p>
          </div>
       </li>
    <% end %>
  </ul>
  <%== pagy_bootstrap_nav(@pagy) %>
<% end %>

<%= link_to "登録する", signup_path %>